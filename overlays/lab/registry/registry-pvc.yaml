- op: add
  path: /spec
  value: 
    storageClassName: '' 
    resources:
      requests:
        storage: 100Gi
    selector:
      matchLabels:
        registry: nfs 
    accessModes:
    - ReadWriteMany
