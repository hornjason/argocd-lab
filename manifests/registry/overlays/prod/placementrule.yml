apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: placement-policy-migrate-registry
spec:
  clusterSelector:
    matchExpressions:
      - key: environment
        operator: In
        values:
          - prod
