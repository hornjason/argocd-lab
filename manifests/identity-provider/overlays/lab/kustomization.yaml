bases:
- ../../base

patchesStrategicMerge:
  - google-oauth-cr.yaml

secretGenerator:
  - name: idp-secret
    behavior: create
    files: 
      - clientSecret
    namespace: openshift-config
    type: Opaque

generatorOptions:
  disableNameSuffixHash: true

